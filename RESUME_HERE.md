# 4주차 진행상황 (세션 중단 대비 — 항상 여기부터 확인)

## 최종 목표
5단계 진행: (1) 연구 방향 구체화 → (2) 앵커논문 활용 기존연구 보완 → (3) 논문 초안 작성 → (4) 실제 멀티에이전트 구현+실험(수치 확보) → (5) 결과 정리 후 논문 수정

## 마감
**2026-08-05 (수) 목표** (오늘 2026-07-27 월 기준 9일 남음)

## 리스크
- 4단계(실제 멀티에이전트 실험)가 최대 병목. 3주차에서 세션 사용량 한도로 65/131 호출에서 중단된 전례 있음.
- 대응: n을 줄이거나(50→20 등), 조건B를 처음부터 진짜 N=3라운드 분리 아키텍처로 설계해 재작업 최소화 검토 중.

## 현재 단계
- [x] 0. 방향 확정: 주제/방법론(멀티에이전트 비판루프 vs 단일LLM, 조건민감 화학설계) **동일 유지**, 3주차는 "연습판"으로 간주하고 **처음부터 새로 제대로** 작성
- [ ] 1. 연구 방향 구체화 — **진행 중** (설계 디테일 확정 필요: n 규모, 조건B를 처음부터 진짜 N=3라운드 분리로 할지, 블라인딩 개선 방법, 기반모델 등)
- [ ] 2. 앵커논문 보완
- [ ] 3. 초안 작성
- [ ] 4. 실험
- [ ] 5. 결과 정리+수정

## 지금까지 정해진 것
- 주제/방법론은 3주차와 동일. 3주차 산출물은 "1차 시도/연습"으로 취급하고 재사용하지 않고 새로 작성.
- 이유: 3주차는 조건B가 원래 설계(N=3라운드 분리팀)가 아니라 셀프시뮬레이션으로 대체 실행됐고, 블라인딩도 실패한 전례가 있음 — 이번엔 처음부터 올바르게.

## 결정사항 로그
- [2026-07-27] 조건B는 처음부터 **진짜 N=3라운드 분리 에이전트**로 구현(셀프시뮬레이션 프록시 금지). 사용자가 "찐 멀티에이전트로 찐 결과"를 명시적으로 원함 → 호출 수가 많아지므로 n 규모를 반드시 그에 맞게 조정해야 함.

## 앵커논문(Virtual Lab) 재확인 요약 (2026-07-27, WebFetch로 원문 재독)
- 에이전트 정의: Title/Expertise/Goal/Role
- 회의 3종: 팀회의(N라운드), 개별회의(+Critic 피드백), 병렬회의(temp 0.8 5회→temp 0.2 병합, 다수결)
- 5단계: ①팀선택 ②프로젝트명세화 ③도구선택 ④도구구현(실제 코드 작성) ⑤워크플로우설계(WS=ESM_LLR+AF_ipLDDT−RS_dG 가중점수, 4라운드 최적화)
- 결과: 92개 나노바디, 발현 38%(고), 결합유지 96%(H11-D4/Nb21)~43%(Ty1), 최고후보 Nb21(I77V-L59E-Q87A-R37Q)
- 인간입력 1.3% vs 에이전트 98.7%(코드 포함)
- 한계: 지식컷오프, 프롬프트엔지니어링 필요성, 도구 자체 부정확성(AF-ipLDDT, Rosetta dG)
- 상세 원문 요약은 이 대화 로그에 있음, 필요시 WebFetch로 재조회 가능 (r.jina.ai 프록시로 biorxiv 403 우회됨)

## FOL 분석 (2026-07-29)
- 앵커논문 핵심주장을 FOL-1로 놓고, 이를 지지하는 사슬(FOL-1→FOL-10, 각 단계가 이전단계+새 사실로 연역)을 구성함.
- FOL-2 = "TeamMeeting(PI,Critic,N-rounds) ⊢ Converges(T,...)" 가 앵커논문에서 **항상 고정된 given**으로 취급되고, 한 번도 X∈{TeamWithCritic, SingleLLM}로 이분화해 비교되지 않았음 — 이게 우리 연구가 개입할 유일한 지점.
- 결론: 연구주제는 기존 H1과 동일(FOL-2를 조작변수화해서 다운스트림 산출물 품질 차이를 본다). 질문 자체는 타당함.

## 3주차 취약점 재진단 (사용자 질문에 대한 답, 2026-07-29)
- 3주차의 진짜 취약점은 "연구주제"가 아니라 "측정 실행": 블라인딩 실패(문체로 90% 식별), 조건B 셀프시뮬레이션 대체, LLM-judge 단독 채점.
- **더 근본적인 미해결 문제(새로 발견, 아직 미확정)**: Virtual Lab은 웻랩 결합실험으로 실제 검증했지만, 우리는 "LLM-judge가 보기에 그럴듯한가"만 측정함 — "실제로 작동하는가"는 검증 불가(실험실 없음).
  - 사용자 확인: 실험실 없어서 직접 합성 검증 불가능.
  - **AI 추천(2안, 사용자 최종결정 대기 중)**:
    1. (필수 권장) 논문 전체에서 claim을 "design plausibility"로 명시적으로 좁히기 — 비용 없음.
    2. (선택 권장) k=1~2(표준반응, 문헌상 정답 명확)에 한해 "문헌 조건 일치 여부"를 LLM-judge 외 보조 객관지표로 추가. k=4~5는 정답 불분명하니 적용 안 함.
    3. (비권장) ChemCrow류 외부 화학 예측 도구 연동 — 9일 일정엔 과함.
  - **미결정**: 사용자가 1번만 할지 1+2번 다 할지 아직 답 안 함.

## 결정사항 로그 (계속)
- [2026-07-29] 웻랩 부재 보강책: **1+2번 다 채택**. Claim은 "design plausibility"로 명시적으로 좁히고, 추가로 k=1~2(표준반응) 과제에 한해 "문헌 조건 일치 여부"를 LLM-judge 외 보조 객관지표로 채점.
- [2026-07-29] H2(조건민감도 상호작용)도 **포함**. 3주차와 동일하게 k=1~5 층화 유지, 상호작용 회귀분석까지 수행.

## 결정사항 로그 (계속)
- [2026-07-29] 기반모델: **Claude(세션 상속)로 확정**. 사용자가 ChatGPT Plus로 직접 대체 실행하는 방안을 검토했으나, 조건B가 과제당 ~9회 호출이라 n=20이면 180회+ 수동 작업이 되어 비현실적이라 기각. OpenAI API(별도 과금) 직접 연동 도구도 없음. GPT-4o와 다르다는 점은 3주차처럼 한계로 명시.
- [2026-07-29] n 규모: **보류(사용자가 결과물 보고 나중에 결정하겠다고 함)**. 일단 진행은 하되, 실제 실행 직전에 반드시 n을 재확인할 것.
- [2026-07-29] 사용자가 "나중에 내가 진짜 원하는 게 뭔지 알려줄게"라고 함 — 아직 완전히 확정된 지향점이 아닐 수 있으니, 큰 되돌리기 어려운 작업(예: 50건 전체 실제 실행)은 사용자의 추가 확인 없이 독단 진행하지 말 것.

## 생성된 파일
- `FOL_기존논문_VirtualLab.md` — 앵커논문 FOL-1~10 사슬 (FOL-2가 항상 고정된 given이라는 한계 명시)
- `FOL_신규논문_4주차.md` — 4주차 논문 FOL-1~10 사슬 (FOL-2를 X∈{TeamWithCritic,SingleLLM} 조작변수화, 앵커와의 대응표 포함, 블라인딩 개선책은 FOL-5에 이미 "서식·분량 통일"로 반영해둠, 웻랩보강책은 FOL-7에 반영)

## 결정사항 로그 (계속)
- [2026-07-29] **순서 재확정**: "초안부터 쓰기" vs "실제 멀티LLM/단일LLM 구현+비교 먼저" 중, 실험을 먼저 하기로 함(불확실성 큰 걸 먼저 처리, paper_writing_skill.md 0절 원칙과도 일치).
- [2026-07-29] 사용자가 "멀티LLM을 직접 구현해보고 싶다 + 남들에게 보여줄 UI도 필요하다"는 새 요구를 명시. 논의 끝에 최종 아키텍처 확정:
  - **데이터 생성(멀티에이전트 실행)**: Claude Code의 Agent/Workflow 도구로 실행(API 키 없음, 사용자가 Anthropic API키도 ChatGPT Plus 대체 실행도 불가하다고 확인함). 조건A(1회)·조건B(7~9회 분리 호출)의 전체 과정을 과제별 JSON 로그로 저장.
  - **프로그램/UI (사용자가 직접 만드는 진짜 소프트웨어)**: 그 JSON 로그를 읽어 보여주는 로컬 웹앱(Flask 등 백엔드 + HTML/JS 프론트). 저는 curl로 백엔드 디버깅 가능, 브라우저 시각 확인은 사용자 몫.
  - **비주얼 컨셉**: "가상 연구실" 느낌으로, 에이전트별 캐릭터를 **고정 자리 + 말풍선** 방식으로 시각화(차례가 되면 해당 에이전트가 하이라이트되고 말풍선으로 발언 표시). 자유이동 애니메이션은 과함으로 판단해 기각.
  - **정직하게 명시할 점**: 멀티에이전트 실행 엔진 자체는 사용자의 독립 API 호출 스크립트가 아니라 Claude Code가 돌린 것 — 3주차와 동일한 "Claude 기반, 세션 상속" 방법론이며 논문에도 이렇게 명시.

## 결정사항 로그 (계속)
- [2026-07-29] `/effort ultracode(xhigh)` 활성화 + `/goal` 설정: "프로그램을 맛깔나게, 실제 멀티LLM으로, 최고로 만들어라". lab_app 빌드 착수.

## lab_app 빌드 현황 (2026-07-29)
- 경로: `4주차/lab_app/` (app.py, templates/index.html, static/style.css, static/app.js, data/transcripts.json, requirements.txt)
- **완료**: Flask 백엔드(`/`, `/api/tasks`, `/api/tasks/<id>`) 작성 및 curl로 정상 동작 확인(200 OK, JSON 정상 파싱). placeholder 데이터로 로컬 테스트 성공, 테스트서버는 종료해둠.
- **완료**: 프론트엔드(가상 연구실 비주얼 — PI/합성화학자/메커니즘전문가/안전전문가/Critic 5석 고정배치 + 중앙 테이블, 조건A용 별도 "Single LLM" 솔로 데스크, 말풍선+재생/이전/다음 컨트롤, judge 채점 막대그래프 패널, 블라인딩 성공/실패 리빌 문구, 다크모드 대응 CSS) 작성 완료.
- **진행 중(백그라운드)**: 실제 멀티에이전트 데이터 생성 — Workflow 실행 중(Run ID: wf_0f4225fc-a52, Task ID: wl2kritp5). 3개 과제(T1 k=1 환원적아민화, T2 k=3 화학선택적환원, T3 k=5 IMDA경쟁반응) 각각에 대해 조건A(1회 호출)+조건B(진짜 분리 에이전트 7회 호출: 초안→비판→개선→비판→개선→비판→PI통합)+블라인드 judge 채점(구조화 출력)을 실제로 실행 중. 완료되면 결과를 `data/transcripts.json`에 덮어써야 함.
- **다음에 반드시 할 일**: 워크플로우 완료 알림 오면 (1) 결과를 `data/transcripts.json`으로 저장 (2) Flask 서버 재기동해 실제 데이터로 curl 검증 (3) 사용자에게 실행 방법 안내(`cd lab_app && python3 app.py` 후 브라우저에서 http://127.0.0.1:5050 접속) (4) 필요시 세부 폴리싱.

## 팀 구조 감사 — 취소됨 (2026-07-29)
- 사용자가 "구조 비교 검증은 지금 필요 없다, 일단 멀티LLM 하나만 만들어달라고 한 거였다"고 정정함 → Workflow 1(wkrx252dl), Workflow 2(wl7ie4xpc) 둘 다 TaskStop으로 중단함.
- 나중에 필요해지면 다시 논의할 수 있으나, 지금은 **실제 데이터 생성(wf_0f4225fc-a52, 5역할·7호출 구조 그대로)만 진행**. 역할 수를 3/5/7 등으로 바꾸는 논의는 보류.

## lab_app 실측 데이터 완료 (2026-07-29)
- Workflow(wf_0f4225fc-a52) 완료. 27개 에이전트 호출 전부 성공(에러 0). T1(k=1)/T2(k=3)/T3(k=5) 3과제 실제 생성 완료.
- `data/transcripts.json`에 실데이터 반영, Flask 서버(curl)로 재검증 완료(/, /api/tasks, /api/tasks/<id> 전부 정상).
- **실측 결과 요약**: n=3, 조건B 우위 2/3, 평균 Gap(B−A)=+0.708, 블라인딩 뚫림 3/3(judge가 매번 조건B를 정확히 식별 — 3주차와 동일 계열의 한계가 이 초소규모 샘플에서도 재현됨). T2(k=3)는 오히려 Gap이 음수(-0.25)로 조건A가 더 높게 채점됨 — 실제 데이터라 억지로 좋게 안 나온 정직한 결과.
- **결과 리포트 Artifact 발행**: https://claude.ai/code/artifact/1b87062d-ceed-40bf-be57-2fc5f839ba7f (과제별 A/B 전체 응답, 루브릭 막대그래프, Gap, 블라인딩 리빌 포함)
- 참고: n=3은 lab_app UI 데모용 표본일 뿐, H1/H2 통계검정을 위한 정식 실험(n 규모는 여전히 보류 중)과는 별개.

## 결정사항 로그 (계속)
- [2026-07-29] **n=20 확정**(k=1~5 각 4건). 사용자가 "찐으로 다 만들어줘, 논문 쓰는 건 다른 데서 할 테니 이번엔 실험+결과자료+보여줄 앱까지"라고 명시 — **이번 4주차 세션의 스코프는 2단계(앵커보완)·3단계(초안작성) 제외, 4단계(실험)+5단계(결과정리+앱)까지만**으로 축소됨.
- [2026-07-29] n=20 실행 착수: 기존 워크플로우(wf_0f4225fc-a52) 스크립트에 새 과제 17개(T4~T20) 추가해 총 20개로 확장. **T1/T2/T3는 기존 캐시 재사용**(동일 prompt 유지) — `resumeFromRunId`로 재실행해 3건은 즉시 캐시 반환, 신규 17건(153회 호출)만 실제로 실행. Task ID: wgns0x3wb.
- 과제 목록(k=1~5 각 4건): k1=[T1,T4,T5,T6], k2=[T7,T8,T9,T10], k3=[T2,T11,T12,T13], k4=[T14,T15,T16,T17], k5=[T3,T18,T19,T20]. 전부 3주차와 겹치지 않는 새로 작성된 과제(반응 유형만 표준 유기화학 지식 기반).

## n=20 1차 시도 실패 (2026-07-29 23:49 KST)
- Workflow wgns0x3wb 완료됐으나 **결과는 전부 실패(20개 과제 전부 pipeline null)**. 180개 에이전트 호출 중 72개만 성공, 108개 실패.
- 원인: **Claude 세션 사용량 한도(11:40pm KST 리셋)에 걸림** — 3주차에서 겪었던 것과 동일한 실패 유형이 재현됨. B4-critic2 단계부터 대부분 "session limit" 에러, judge 호출은 전부 실패.
- 부수적으로 "Connection closed mid-response" API 에러도 8건 발생(세션한도와 무관한 일시적 네트워크 오류).
- ultragoal G001을 `failed`로 체크포인트 기록함(evidence 남김).
- **한도가 23:40 KST에 리셋됐고 지금(23:49) 이미 지난 시점**이라 바로 재시도 착수: Workflow(wfc1cxaq5, 동일 run wf_0f4225fc-a52 resume)로 재실행 중. 캐시 재사용 가능한 부분(72개 성공분 중 일부)은 즉시 반환되고 나머지는 새로 실행됨.

## 전략 전환: 배치 분할 실행 (2026-07-29 23:5x, 사용자 제안 반영)
- 사용자가 "n=20을 나눠서 진행하면 안되냐"고 제안 → 통짜 20건(180호출) 재시도(wfc1cxaq5)는 TaskStop으로 중단.
- 스크립트에 `args`로 과제ID 필터링하는 배치 기능 추가(`const BATCH = args?.length ? TASKS.filter(...) : TASKS`).
- **5배치 계획(k레벨별 4건=36호출/배치)**: 배치1=[T1,T4,T5,T6](k1), 배치2=[T7,T8,T9,T10](k2), 배치3=[T2,T11,T12,T13](k3), 배치4=[T14,T15,T16,T17](k4), 배치5=[T3,T18,T19,T20](k5).
- 배치1 실행 중: Task ID w5akcn9ls, Run ID wf_6c371b85-0c4 (스크립트에 args필터 추가하며 새 run ID로 분리됨 — 이전 run wf_0f4225fc-a52의 캐시는 이 배치들에 재사용 안 됨, 매 배치 새로 실행).
- 각 배치 완료될 때마다: (1) 결과를 로컬 배열에 누적(예: `lab_app/data/batch1.json` 등으로 개별 저장 후 나중에 합침, 3주차의 batch1~5 방식과 유사) (2) 다음 배치 바로 이어서 실행.

## 중대 버그 발견: 배치 필터가 작동 안 했음 (2026-07-30 08:34 KST)
- 배치1(w5akcn9ls) 결과 확인해보니 **agent_count=180, T1~T20 전부 참조** — 즉 4건(36호출)이 아니라 20건(180호출) 전체가 또 실행됐었음. args 필터가 무효화됨.
- 원인 추정: Workflow 호출 시 넘긴 `args`가 배열이 아니라 JSON 문자열로 전달되어 `Array.isArray(args)`가 false가 되고 `TASKS`(전체) 폴백으로 빠짐.
- **수정**: 스크립트에서 `args`가 문자열이면 `JSON.parse`로 변환하도록 방어 코드 추가 + `log()`로 실제 필터링된 BATCH 목록을 출력하도록 계측 추가.
- **검증 중**: 수정 후 바로 큰 배치를 또 돌리면 위험하니(6시간+ 낭비 반복), 우선 **1건(T4)짜리 초소형 테스트**(Task w8cjc8wl0, Run wf_d8c7b417-78a)로 필터가 진짜 4건/1건으로 줄어드는지 log 메시지와 agent_count로 먼저 확인.
- 이 검증 없이 배치 크기를 다시 늘리지 말 것 — 지난 두 번의 대량 실패(180회 시도 2회, 총 6시간+ 소요, 완주 0건)를 반복하면 안 됨.

## 버그 수정 확인됨 (2026-07-30 09:1x KST)
- T4 1건 테스트(w8cjc8wl0) 결과: **agent_count=9, agents_done=9, agents_error=0** — 필터 정상 작동 확인. 소요시간 약 33분/task.
- T4 완주 데이터를 `lab_app/data/batch_T4.json`에 저장해둠(재사용, 재실행 안 함).
- 배치1 나머지(T1,T5,T6, 27호출) 실행 중: Task wb31rk3bf, Run wf_0176a3ce-984.
- 참고 페이스: task당 약 30~35분 소요. 배치(4건)는 동시 실행되므로 대략 30~40분 내외로 끝날 것으로 예상(병렬 처리).

## 배치1 완료 (2026-07-30) ✅
- T1,T4,T5,T6 (k=1) 전부 완주. `lab_app/data/batch1.json`에 저장됨.
- 결과: T1 gap=+1.25, T4 gap=+2.25, T5 gap=+1.25, T6 gap=+2.5 — **4/4 전부 B우위**, 블라인딩은 4/4 전부 뚫림(judge가 매번 조건B 정확히 식별).
- 배치2(T7,T8,T9,T10, k=2) 실행 중: Task w1b47e2pf, Run wf_5b17369a-437.

## 배치2 완료 (2026-07-30) ✅
- T7,T8,T9,T10 (k=2) 전부 완주(36/36, 에러0). `lab_app/data/batch2.json` 저장.
- 결과: T7 gap=+0.5, T8 gap=+1, T9 gap=+1, T10 gap=+1.125 — 4/4 전부 B우위, 블라인딩 4/4 뚫림.
- 배치3(T2,T11,T12,T13, k=3) 실행 중: Task w83wrnh5p, Run wf_8c5f5d7e-767.

## 배치3 부분 성공 + 데이터 손상 버그 발견 (2026-07-30)
- 배치3(T2,T11,T12,T13) 35/36 성공, 1개 실패(B1-draft:T11, connection error).
- **중요 버그 발견**: T11의 pipeline 결과가 null이 아니라 손상된 값으로 채워짐 — B1-draft 호출이 null을 반환했는데 스크립트가 이를 체크 안 하고 그대로 다음 프롬프트(critic1 등)에 흘려보내 "null 초안을 비판/개선"하는 가짜 체인이 완주됨(Gap=-4라는 비정상값으로 발견). **T11 데이터는 폐기**하고 `batch3_partial.json`에 T2,T12,T13만 남김.
- **스크립트 수정**: `assertOk()` 헬퍼 추가 — 각 agent() 호출 결과가 null/빈 값이면 즉시 throw해서 pipeline이 해당 과제를 정상적으로 null 처리(재실행 대상)하도록 방어. 이 수정 전에 생성된 데이터 중 이런 손상이 더 있었을 가능성은 낮음(다른 실패는 전부 명확히 null로 처리됐었음 — T11 케이스만 유일하게 "부분 성공처럼 보이는 손상"이었음).
- T11만 재시도 중: Task wkmpwryta, Run wf_e9c1c8e5-412 (수정된 스크립트 사용).
- 배치3 결과(T2,T12,T13): T2 gap=+1.125, T12 gap=+1.25, T13 gap=+1.5 — 전부 B우위, 전부 블라인딩 뚫림.

## 배치3 완성 (2026-07-30) ✅
- T11 재시도 9/9 성공(gap=+1.25, guessedB=True). `lab_app/data/batch3.json`에 T2,T11,T12,T13 전부 저장 완료.
- 배치4(T14,T15,T16,T17, k=4) 실행 중: Task wx4ji7m22, Run wf_6ed7cefb-3e0 (assertOk 방어코드 적용된 스크립트).

## 배치4 1차 시도 실패 → 재시도 (2026-07-30)
- 배치4(T14~T17) 4건 전부 "Connection closed mid-response" 일시적 오류로 실패(세션한도 아님). **assertOk 방어코드가 정상 작동해 손상 없이 깔끔하게 null 처리됨을 확인**(T11때와 달리 이번엔 데이터 오염 없음).
- 동일 배치(T14~T17) 재시도 중: Task wlpym6rxj, Run wf_b3faac04-8ac.

## 배치4 2차 시도: T15,T16 성공 / T14,T17 재실패 (2026-07-30)
- T15 gap=+0.75, T16 gap=+1.5, 둘 다 guessedB=True. `batch4_partial.json`에 저장.
- T14, T17은 또 "Connection closed mid-response"로 실패(세션한도 아닌 순수 네트워크 일시오류로 보임, assertOk로 안전하게 null 처리됨).
- T14,T17만 재시도 중: Task w5olj0oj9, Run wf_fe97221f-f6e.

## 배치4 완성 (2026-07-30) ✅
- T14 gap=+1.5, T17 gap=+1.5, 둘 다 guessedB=True. batch4.json에 T14,T15,T16,T17 전부 저장 완료.
- **배치5(마지막, T3,T18,T19,T20, k=5) 실행 중**: Task w5ehvg8wf, Run wf_cf5fedf0-409. 이거 끝나면 20건 전부 완료.

## ★ n=20 실험 완전 종료 (2026-07-30) ★

**최종 통계 (transcripts.json, stats.json)**
- **H1 (팀이 단일LLM보다 나은가)**: **지지됨.** B우위 18/20(90%), Wilcoxon p=0.00019, Cohen's dz=1.61(매우 큼)
- **H2 (조건민감도 k가 높을수록 우위가 커지는가)**: **지지 안 됨 — 방향 반전.** slope=-0.266, p=0.011(유의하지만 음수). k=5 평균 Gap(0.281)이 5구간 중 가장 작음. k별 평균: k1=1.812, k2=0.906, k3=1.281, k4=1.312, k5=0.281.
- **블라인딩**: **완전 실패.** 20/20(100%) judge가 조건B를 정확히 식별(binomial p=2e-6) — 3주차(90%)보다도 심함. 조건B가 이번엔 진짜 분리 에이전트였음에도 재현·악화됨.
- 이 패턴은 3주차(셀프시뮬레이션 프록시 사용)와 **정성적으로 동일** — H1 강지지, H2 반전, 블라인딩 실패. 다만 이번엔 진짜 멀티에이전트로 재현했다는 점에서 3주차보다 방법론적으로 견고한 반복검증임.

**과정에서 실제로 있었던 일 (전부 기록, 축소 없음)**
- n=20 통짜(180호출) 시도 2회 세션한도로 실패 → 사용자 제안으로 5배치(각 4건, 36호출) 분할 전환
- 배치 필터 버그(args가 문자열로 전달되어 매번 전체 20건이 실행되던 문제) 발견·수정
- T11 데이터 손상 버그(초안 호출 null이 조용히 다음 단계로 전파) 발견 → 폐기 후 재실행, assertOk 방어코드 추가
- 이후 배치들에서도 "Connection closed mid-response" 산발적 실패 있었으나 방어코드 덕에 안전하게 null 처리 → 실패분만 재시도로 전부 회수
- Artifact 배포 중 데이터 인코딩 손상 1건(U+FFFD) 추가 발견·수정

**최종 산출물**
- `lab_app/data/transcripts.json` — 20건 전체 실데이터(진짜 멀티에이전트, 셀프시뮬레이션 아님)
- `lab_app/data/stats.json` — H1/H2/블라인딩 전체 통계
- lab_app Flask 앱 — 20건 실데이터로 curl 검증 완료, `cd lab_app && python3 app.py` → http://127.0.0.1:5050
- 결과 리포트 Artifact(n=20 통계 포함, 재발행): https://claude.ai/code/artifact/1b87062d-ceed-40bf-be57-2fc5f839ba7f
- ultragoal: G001(실패, 이력 보존)/G002/G003/G004 — 4개 중 3개 complete

## lab_app UI 반복 개선 히스토리 (2026-07-30~31, n=20 실험 완료 후)
사용자가 "UI가 맘에 안든다"며 여러 차례 갈아엎기를 요청, 매번 방향이 달라짐. 순서대로:
1. 미니멀 대시보드(카드+막대그래프) → "예쁘지 않다" 반려
2. 완전 재설계: 좌(콜드 CRT터미널) vs 우(코르크보드 팀) 대비 컨셉 → "괜찮은데 캐릭터/거리감/실험실 느낌 부족" 피드백
3. **AI-town류 진짜 도트 캐릭터 요청** → SVG로 픽셀 스프라이트 직접 그림(12x18→16x22 grid), 방을 벽/바닥 2단 구조로 재설계(이전엔 poster가 overflow:hidden에 잘려 안 보이던 버그 발견·수정)
4. "대형 연구소같지 않다" → 벽에 연구소 안내판(다른 랩 목록+현재위치), 흄후드·싱크대·약품장·화이트보드·소화기 등 실험기구 추가, 자율 이동(랜덤 목적지 배회) 구현
5. **"움직임이 픽셀이 아니라 종이가 움직이는 느낌"** → 대대적 기술 개선 라운드: (a) 이동을 smooth easing→stepped(steps())+4px 격자정렬로 변경 (b) 스프라이트 렌더링을 SVG→Canvas(무앤티에일리어싱)로 전환 (c) 걷기 홉+정지시 idle 흔들림 추가 (d) 모든 border-radius 제거(각짐 통일) (e) 전체 색상 채도 대폭 상승
- **현재 상태(2026-07-31)**: 5번 라운드의 기술적 개선사항 전부 구현·서버검증(curl 200) 완료했으나, **사용자가 아직 실제로 새로고침해서 확인/피드백을 준 적이 없음** — `/goal` Stop-hook이 "게임스럽고 영상스러운 느낌 검증"을 조건으로 걸어놨고 이건 사용자의 시각적 확인 없이는 충족 불가능. 여러 차례 확인 요청했으나 응답 없어 무작정 코드만 계속 바꾸는 건 비효율적이라 판단, 대신 이 진행상황 정리로 전환함.
- **다음 세션에서 이어갈 때**: 사용자가 실제로 화면을 보고 구체적 피드백(어떤 부분이 안 게임같은지)을 주면 그에 맞춰 조정. 이미 시도한 가설(이동방식/렌더링/모서리/채도)은 전부 소진했으므로, 다음 가설은 사용자 피드백에 의존해야 함 — 근거 없이 더 추측성 변경을 반복하지 말 것.
- **[2026-07-31 업데이트] 레퍼런스 리서치로 방향 수정**: 사용자가 요구한 스타일이 정확히 무엇인지 WebSearch로 조사한 결과 **"AI Town"(a16z-infra, Stanford Generative Agents 기반 오픈소스)**임을 확인. 실제 구현은 PixiJS + 32x32 전문 스프라이트시트("32x32folk.png") + **매끄러운(smooth/linear) 실시간 이동**(steps() 아님!). 즉 이전 라운드에서 "stepped 이동"으로 바꾼 게 실은 **잘못된 방향**이었음 — 되돌려서 `linear` 타이밍으로 수정함. "청키함"은 이동방식이 아니라 스프라이트 아트 디테일에서 나오는 것이었음(이 부분은 이미지 생성/에셋 다운로드 도구가 없어 완전히 재현 불가, 손으로 그린 16x22 단순 캐릭터로는 한계 있음을 사용자에게 정직히 고지함).
- 추가로 AI Town/생성형 에이전트 시뮬레이션의 관례인 **NPC 머리 위 말풍선**을 구현(기존엔 이모지만 떠 있었는데, 배경+꼬리 있는 진짜 말풍선 박스로 교체).
- **여전히 사용자의 실제 확인 대기 중** — 이 교정이 맞는 방향인지도 사용자가 봐야 알 수 있음.

## [2026-07-31 추가 업데이트] 실제 CC0 캐릭터 스프라이트로 교체 + 자체 시각검증 완료
- 손그림 비트맵 대신 **Kenney "RPG Urban Pack"(CC0, kenney.nl)의 진짜 캐릭터 타일셋**을 다운로드해 6종 캐릭터(각 2프레임 걷기모션) 중 5종을 팀 역할에 배정: pi=노년 회색머리, synthetic_chemist=초록셔츠 소년, mechanistic_chemist=빨간셔츠 장발, safety_specialist=밀짚모자 오버올, critic=두건 쓴 캐릭터. 파일은 `lab_app/static/sprites/<role>_0.png`/`_1.png` (16x16 raw, CSS로 96px 크리스프 확대).
- `sprites.js` 전면 재작성: 손그린 HEADS/TORSO/LEGS 비트맵 배열 전부 제거, `<img>` 기반 프레임스왑으로 교체. 로봇(SOLO 씬)만 기존 캔버스 그대로 유지(이 팩엔 로봇이 없어서).
- **자체 시각검증 인프라 구축**: `pip3 install playwright && playwright install chromium`으로 헤드리스 브라우저를 처음으로 설치, 실제 페이지를 스크린샷 찍어 Read 툴로 직접 봄(이전엔 코드만 보고 "될 것 같다"고 추측했던 것과 질적으로 다른 검증 방식). 앞으로도 이 환경엔 playwright가 남아있으니 UI 작업 시 재사용 가능.
- 스크린샷으로 실제 버그 2건 발견·수정(추측이 아니라 렌더링 결과를 보고 확정):
  1. **name-tag가 캐릭터 배회(wander) 이동을 따라가지 않던 버그**: `moveTo()`가 `.sprite-anchor`에만 transform을 적용하고 형제 요소인 `.name-tag`는 그대로 둬서, 캐릭터가 이동하면 이름표만 원래 자리에 남아 시각적으로 분리됨(정확히 "싸구려 느낌"의 원인 중 하나였을 가능성 높음). `moveTo()`가 이제 name-tag에도 동일한 translate(스케일X 제외, 텍스트 안 뒤집히게)를 같이 적용하도록 수정.
  2. **벽 근처 캐릭터(mount-sc, mount-mc)가 배회 중 방 경계 밖으로 나가 overflow:hidden에 잘리던 버그**: 새 스프라이트가 기존 손그림(64x88)보다 커진 96x96이라, 기존 고정폭 배회범위(±70px)로는 벽에 너무 가까운 캐릭터가 잘려서 얇은 조각만 보였음. `captureBaseRects()`로 각 캐릭터의 실제 방 안 위치를 기록하고, `wanderBounds()`로 방 경계를 절대 벗어나지 않게 배회범위를 동적으로 제한하도록 수정. 8초간 반복 측정해 클리핑 0건 확인.
  3. (오탐 2건도 확인 후 기각: 화이트보드의 "반응 화살표 낙서" 장식을 버그로 착각했다가 CSS 확인 후 의도된 디자인임을 확인함. 스크린샷의 "얇은 세로선" 중 일부는 CSS transition 도중에 풀페이지 스크린샷을 찍을 때만 생기는 촬영 아티팩트였고, transition/animation을 강제로 멈춘 뒤 다시 찍으니 5명 전부 정상 렌더링됨 — 실제 사용자 화면에서는 발생하지 않는 문제로 판단.)
- 콘솔 에러/실패한 네트워크 요청 0건, 팀/솔로/캠페인 3개 씬 전환 정상 확인.
- **이 라운드는 사용자의 실제 확인 없이 스크린샷 근거로 마무리** — 여전히 사용자가 http://127.0.0.1:5050 을 직접 보고 "이제 원하던 느낌인지" 확인해줘야 `/goal` 조건이 완전히 해소됨.

## [2026-07-31 추가] 바닥을 CSS 그라디언트 → 진짜 픽셀타일로 교체
- `/goal` Stop-hook이 같은 피드백으로 재차 실행됨(사용자가 아직 화면을 보지 않은 상태에서의 자동 재알림). 사용자 응답을 기다리는 동안 손 놓고 있지 않고, 스스로 스크린샷을 다시 비판적으로 뜯어봄.
- 발견: 캐릭터는 이제 진짜 픽셀아트인데 **바닥은 여전히 CSS 대각선 그라디언트 체커 패턴**이라 스타일이 어긋남(크리스피한 픽셀 캐릭터 vs 벡터 느낌 바닥) — 이게 "여전히 게임같지 않다"의 실질적 원인 중 하나일 가능성이 높다고 판단.
- Kenney 타일맵에서 진짜 회청색 포장타일(plain, seamless 확인됨)을 크롭해 `lab_app/static/tiles/floor.png`로 저장, `.room-tiles`/`.room-tiles.dim`의 background를 이 이미지로 교체(dim은 `filter: brightness(0.4)`로 어둡게). 8x8 타일 미리보기로 이음새 없음을 먼저 확인한 뒤 적용.
- 벽(`.room-wall`)은 그대로 둠 — 이미 조명/창문 연출이 있어 리스크 대비 효과가 낮다고 판단, 후보 벽돌 타일도 찾아뒀지만(같은 팩) 색감이 기존 어두운 네이비 분위기와 안 맞아 보류.
- 스크린샷 재검증: 바닥이 훨씬 "진짜 게임 화면"처럼 보임. 콘솔에러 0, 배회 클리핑 0(8초) 재확인.
- **여전히 사용자의 실제 확인 대기 중** — 이것도 사용자가 직접 봐야 판단 가능.

## [2026-07-31 추가] 걷기 애니메이션 속도가 실제 이동속도와 따로 놀던 버그 수정
- `/goal` hook 재발화(사용자 응답 없는 자동 재알림) 동안 또 대기만 하지 않고, 사용자가 명시한 "움직임이 종이가 움직이는 느낌"이라는 문구를 다시 곱씹어 코드 레벨에서 원인을 찾음.
- 원인 발견: 캐릭터 몸체 이동(`transition: transform Ds linear`, 거리에 비례한 D)과 다리 스왑/홉 애니메이션(`0.5s`/`0.28s` 고정 주기)이 서로 완전히 무관하게 돌고 있었음 — 느리게 멀리 이동할 때도 다리는 항상 같은 속도로 왔다갔다해서 "몸은 미끄러지고 다리만 따로 논다"는 인상을 줬을 가능성. 실제 걷는 것처럼 안 보이고 이미지가 슬라이드하는 것처럼 보이는 전형적 원인.
- 또한 기존 배회 이동시간 클램프(`[0.7s, 2.4s]`)가 실제 배회범위(0~~100px)를 좁은 시간대에 욱여넣어 속도가 들쭉날쭉했음(짧은거리도 0.7s, 긴거리도 2.4s로 뭉개짐).
- 수정: (1) `--walk-rate` CSS 변수를 이동 duration에서 계산해 다리스왑/홉 애니메이션 주기가 실제 이동속도에 비례하도록 동기화, (2) 배회 속도를 ~60px/s로 거의 일정하게(클램프는 극단값 방지용으로만 완화: `[0.35s, 2.2s]`).
- Playwright로 `--walk-rate` 값이 매 이동마다 실제로 다르게 계산되고 반영됨을 확인(0.29~1.06 범위 관측), 콘솔에러 0, 클리핑 0(10초) 재확인.
- **여전히 사용자의 실제 확인 대기 중.**

## [2026-07-31 추가] 첫 실제 구체 피드백 반영 — 가운/회의테이블/버너
사용자가 드디어 구체적 피드백을 줌(그 전까지는 `/goal` 훅 재발화만 반복, 새 입력 없었음): "실험실 같지 않다", "캐릭터들이 광부같다 → 가운 입은 사람들로", "중간 책상을 회의실 책상으로 더 크게", "주변을 좀 더 실험실스럽게", "지금 루프 도는 것 같은데 확인해서 고쳐".
- **가운(lab coat) 처리**: Kenney 스프라이트 5종 모두 몸통/하의 영역(row10-14, 캐릭터 실루엣 alpha 기준)을 흰색/연회색으로 재채색하는 PIL 스크립트 작성(윤곽선·피부색은 프레임0의 얼굴/손 영역에서 뽑은 기준색으로 보존, 걷기 프레임 간 오염 방지를 위해 기준색은 항상 frame0에서만 추출). 결과: 5명 전부 흰 가운 입은 것처럼 보임. (파일: `static/sprites/*.png` 덮어씀, 원본은 tilemap에서 재생성 가능)
- **회의실 테이블**: `.table-block` 108x68 → 190x100으로 확대, 서류더미(`.table-papers`)·두번째 비커(`.table-flask-2`)·노트북(`.table-laptop`, 3D transform 없이 완전 flat) 추가.
- **실험실 분위기 강화**: 분젠버너 2개(`.burner-station`) 하단 중앙에 추가(불꽃 box-shadow glow) — "이건 진짜 실험실이다"를 가장 직관적으로 말해주는 오브젝트.
- Playwright로 재검증: 콘솔에러 0, 8초간 클리핑 0, 스크린샷으로 레이아웃 겹침 없음 확인.
- `/goal` 루프에 대해서도 사용자에게 설명함: OMC 자체 모드는 전혀 안 걸려있음(cancel skill로 확인, 전부 inactive) — 이건 Claude Code 네이티브 `/goal`이고, 조건이 "사용자 육안 확인"이라 내가 프로그램적으로 절대 해제 못 함. 사용자가 `/goal`을 다시 실행하면 직접 해제 가능하다고 안내함.
- **여전히 사용자의 실제 확인 대기 중** — 이번엔 진짜 구체적 요청사항 3가지에 대응했으므로 다음 피드백이 더 정확할 가능성 높음.

## ★ [2026-07-31] 사용자 육안 확인 완료 — 현재 UI 확정(baseline) ★
- 사용자가 http://127.0.0.1:5050 에 직접 접속해서 보고 **"일단 이거 유지"**라고 승인함. 여러 라운드 반복됐던 "UI가 맘에 안든다" 사이클은 여기서 종료.
- 즉 **현재 lab_app 상태(흰 가운 캐릭터 5종 + 확대된 회의테이블 + 분젠버너 + 픽셀 바닥타일 + linear 이동 + walk-rate 동기화)가 확정 baseline**임.
- **중요**: 앞으로 UI를 추측성으로 다시 갈아엎지 말 것. 사용자가 승인한 상태이므로, 변경은 사용자가 명시적으로 요청한 부분에만 국한할 것(기존 승인분은 보존).

## ★ [2026-07-31] 논문 작성 전 최종 점검 (데이터 직접 검산) ★

**검산해서 통과한 것**
- `stats.json` 1차 통계 전부 재현됨(mean gap 1.11875, W=2.5, dz=1.608, H2 slope=-0.2656 p=0.0113, 블라인딩 20/20 p=1.9e-6).
- **강건성 검정 추가 통과(중요)**: safety 차원을 빼고 3차원(reagent/condition/mechanism)만으로 재분석해도 결론 유지 — H1 여전히 유의(mean gap 0.792, Wilcoxon p=0.0057, dz=0.906), H2 여전히 반전(slope=-0.3375, p=0.0101). 즉 아래 confound 때문에 결론이 뒤집히지는 않음.
- 순서효과 없음: Mann-Whitney p=0.41.
- 실험 스크립트를 프로젝트 안으로 회수: `4주차/실험재현자료/실험스크립트_최종.js` (이전엔 `~/.claude` 세션 디렉토리에만 있어서 소실 위험이었음). 과제 20개 전문, 5역할 정의, 7단계 프롬프트, judge 루브릭·스키마, assertOk 방어코드 전부 포함 = Methods 절 1차 근거자료.
- 검산 결과를 `lab_app/data/stats_verified.json`으로 저장(차원별 분해, 3차원 강건성, 순서효과, 과제별 gap 표 포함).

**논문에 반드시 명시해야 할 것 (검산에서 새로 드러남)**
1. **safety 차원 confound (가장 중요)**: 전체 Gap의 대부분이 safety 한 차원에서 나옴 — 차원별 diff가 reagent +0.875 / condition +0.800 / mechanism +0.700 인데 **safety만 +2.100**. 조건A의 safety 점수는 20건 중 18건이 2~3점, 조건B는 18건이 5점. 원인은 구조적임: 조건B 파이프라인에 "공정안전 전문가" 에이전트가 명시적으로 들어있고 조건A에는 없음. 즉 "팀이 더 낫다"의 상당 부분은 **비판루프 효과가 아니라 안전 역할을 팀에만 넣은 설계 비대칭** 때문일 수 있음. → 논문에서 (a) 이 confound를 명시하고 (b) safety 제외 3차원 강건성 결과를 같이 보고할 것.
2. **Wilcoxon p가 두 값 존재**: 동점(gap=0) 1건 때문. 정규근사(n=20 포함) p=0.00019 / 정확검정(동점 제외 n=19) p=1.9e-05. 어느 쪽을 쓰든 결론 동일하나 **논문에 어느 방법인지 반드시 명기**할 것. stats.json에 기록된 값은 정규근사쪽.
3. **채점 순서 불균형 12/8**: 원래 index%2로 교차배정했으나 배치 분할·실패분 재실행 때문에 B_first 12 / A_first 8이 됨. 순서효과 자체는 유의하지 않음(p=0.41)이라 치명적이지 않지만 한계로 명시.
4. **judge가 단일 호출·근거 미저장**: judge는 과제당 1회 호출이고 채점 rationale을 저장하지 않았음 → 채점자 간 신뢰도(inter-rater reliability) 측정 불가. 한계로 명시.

**미이행 결정사항 → 이번에 해소함**
- [2026-07-29] "웻랩 부재 보강책 1+2번 다 채택" 중 **2번(k=1~2 문헌일치 보조 객관지표)이 원래 구현 안 돼 있었음**(judge 필드에 관련 항목 없음 = LLM-judge 단독 채점, 3주차 취약점 그대로). 사용자 승인으로 **2026-07-31 실행 완료**.
- 1번(claim을 design plausibility로 좁히기)은 논문 서술 단계 사안이라 아직 해당 없음.

## ★★ [2026-07-31] 문헌일치 보조지표 실행 완료 — 논문의 핵심 반전 결과 ★★

**방법 (Methods에 이렇게 쓸 것)**
- 대상: k=1~2인 8과제(T1,T4,T5,T6,T7,T8,T9,T10). k=3~5는 문헌상 정답이 불분명해 제외(7/29 결정대로).
- **answer key 사전 확정**: 각 과제마다 문헌 표준조건을 4항목((a)시약/촉매 (b)용매 (c)온도 (d)선택성 전략)으로 미리 명문화한 뒤 채점 — 사후 자유판정보다 객관적이게 함. key 전문은 `4주차/실험재현자료/문헌일치_answer_key.md`.
- **블라인드 유지**: 조건 라벨을 제거하고 원래 judge와 동일한 제시순서로 "제안1/제안2"만 담은 파일을 만들어 채점자에게 제공. 채점자는 다른 파일 열람 금지 지시.
- 채점자: 과제별 독립 에이전트 8개(병렬), 각 항목 1(일치)/0(불일치) 이진 채점. 결과는 `lab_app/data/lit_match.json`.

**결과 (LLM-judge 루브릭과 정반대)**
- 조건A(단일LLM) **30/32 = 93.8%**, 조건B(팀) **29/32 = 90.6%**. 평균 lit_gap(B−A) = **−0.125**.
- 과제별 승패: B우위 1건(T5), 동점 6건, **A우위 1건(T7)**. 동점 제외 n=2로 검정력 없음(Wilcoxon p=1.0) — 즉 **차이 없음**.
- 대조: 같은 8과제에서 LLM-judge 루브릭 평균 gap은 **+1.359로 B가 8/8 전승**이었음.
- 개별 불일치 사유: T5는 A가 그리냐르 당량 미제시로 −1. **T7은 조건B가 모노-Boc 전략을 '모노양성자화+Boc2O 1당량'으로 잡고 용매도 MeOH/수계로 제시해 문헌표준(다이아민 대과량·DCM계) 대비 (a)(c) 둘 다 불일치 −2** — 팀이 오히려 비표준 경로로 이탈한 사례. T8은 A·B 모두 Ac2O 당량을 0.2~0.35로 제시해 문헌기준(0.9~1.0당량)과 불일치.

**논문에 이게 왜 중요한가 (해석 — Discussion 핵심 소재)**
- H1("팀이 낫다")은 **LLM-judge 루브릭에서만 성립하고, 문헌일치라는 객관지표에서는 사라짐**. 두 지표가 서로 다른 방향을 가리킴.
- 가장 설명력 있는 해석: LLM-judge의 조건B 선호는 **문헌적 정확성이 아니라 응답의 포괄성·상세함(특히 안전 항목 추가, 서술량)** 을 반영한 것일 수 있음. safety 차원 confound(+2.100 vs 나머지 +0.7~0.9)와 블라인딩 100% 실패(judge가 20/20 조건B 식별)가 이 해석을 뒷받침함 — judge는 "더 숙고한 티가 나는 답"을 알아보고 그쪽에 점수를 준 것.
- 즉 **본 연구의 진짜 기여는 "팀이 낫다"가 아니라 "LLM-judge 기반 멀티에이전트 평가가 체계적으로 팀에 유리하게 편향된다"는 방법론적 경고**가 될 수 있음. 이 방향이 Virtual Lab류 연구(웻랩 검증 없이 LLM 평가만 쓰는 경우)에 대한 비판적 기여로 더 강함.
- 단, n=8·동점 6건이라 "A가 더 낫다"고 주장하면 과대해석. **정확한 서술은 "문헌일치 지표에서는 두 조건 간 유의한 차이가 관찰되지 않았다"**.

## ★ [2026-07-31] 문서 내부모순 점검 및 정정 ★
사용자가 "완벽해?"라고 물어 설계문서를 실측과 대조했고, 어긋난 곳을 정정했음.
- `FOL_신규논문_4주차.md`: 말미에 **"⚠️ 실측 결과와의 대조"** 절 추가. FOL-5(블라인딩 원인 제거 실패)·FOL-7(두 지표 어긋남)·FOL-8(다중검정 보정 안 했음)·FOL-9(H2 예상과 달리 기각)·FOL-10(전제 3개 중 2개 거짓 → 최종 주장문 재작성 필수) 5개 단계를 표로 대조. 사슬에 빠져있던 safety confound·순서 불균형도 추가. 미결정 2건(n, 문헌일치 기준)은 해소 처리.
- `paper_writing_skill.md`: (1) 4절 예문에 박혀있던 **3주차 수치("47/50=94.0%, p<0.0001")를 4주차 실측값(18/20=90.0%, p=0.00019)으로 교체** — 그대로 베끼면 허위보고였음 (2) 5절의 FOL 참조 오류 수정(스코프 한정 문구는 FOL-7이 아니라 FOL-10에 있음) + FOL 인용 전 대조절 먼저 읽으라는 경고 추가 (3) 7절 체크리스트에 실측에서 새로 드러난 필수항목 10개 추가(safety confound, 3차원 강건성, 두 지표 불일치, 과대해석 금지, Wilcoxon 방법 명기, 다중검정 허위기재 금지, 블라인딩 정직서술, 순서 불균형, judge 신뢰도, 재현자료 경로).

**FOL-10 최종 주장 프레이밍 → 결정·재작성 완료 (2026-07-31)**
- 사용자가 **"편향 경고로 전환"**을 선택함. 새 주장: *"LLM-judge 루브릭으로 측정한 팀의 우위는 상당 부분 평가 방법에서 비롯된 산물이다 — 블라인딩 20/20 실패, Gap의 safety 편중, 문헌일치 지표에서의 차이 소멸. 따라서 웻랩 검증 없이 LLM-judge 점수만으로 멀티에이전트 우월성을 주장하는 평가 설계는 과학적 정확성이 아니라 응답의 포괄성·상세함을 측정하고 있을 가능성이 있다."*
- 폐기된 이전 주장문은 FOL-10 절에 취소선으로 기록 보존함(이력 삭제 안 함).
- 이 프레이밍은 **Title과 Introduction "기여 3가지"에도 반영 필요** — 질문형 제목은 "팀이 더 나은가?"가 아니라 "우리는 무엇을 측정하고 있는가?" 계열이 데이터와 맞음. paper_writing_skill.md 5절에 지침 반영해둠.
- **주의: "단일LLM이 팀보다 낫다"로 쓰면 과대해석**(문헌일치 n=8·동점 6건, 검정력 없음). H1도 3차원 강건성에서 유지되므로 "H1이 허위"라고도 쓰지 말 것.

## ★★★ [2026-07-31] 논문 완성 — `논문_4주차_최종.md` ★★★
사용자가 `/goal`로 "new FOL을 모두 만족하는 논문을 paper_writing_skill로 써서 완성"을 지시 → 집필 완료(382행, 약 5,200단어).

**구성**: 질문형 제목+부제 / Abstract / 1.Introduction(기여 3가지) / 2.재사용 아키텍처(에이전트 5종 4요소 표) / 3.Method(앵커 5단계 1:1 대응표 + FOL 단계 대응표 + 3.1~3.7) / 4.Results(4.1 H1 ~ 4.7 문헌일치, 표 4개) / 5.Discussion(해석·방법론적 기여·한계·종합주장·일반화·다루지 않은 개념) / 6.Conclusion(3문장) / References(8건) / Appendix A~D(프롬프트·루브릭·재현자료·용어해설)

**FOL 커버리지**: FOL-1~10 전부 Method/Results 소절에 1:1 매핑(3.0.1 표). 재작성된 FOL-10 편향경고 주장을 5.4에 그대로 반영.

**검증 완료 (기계적 대조)**
- 수치 13종 전부 `stats_verified.json`/`lit_match.json`과 일치 확인(mean gap 1.119, sd 0.696, W 2.5, dz 1.61, H2 slope −0.266/r −0.554/p 0.011, 3차원 0.792/0.873/27.5/0.0057/0.906/−0.338). 표1 과제별 Gap 20건 전부 일치(불일치 0건).
- 승패(18/1/1, 3차원 17/1/2), 블라인딩 20/20, 순서 12/8·p=0.413, 차원별 4종, k별 5종, 문헌일치 30·29/32 전부 대조 통과.
- skill 규칙 준수: 미래형·계획체 0건(오탐만), 취소선 0건, 3주차 수치 잔존 0건, Conclusion 3문장, 음수 표기 U+2212로 통일.
- skill 7절 체크리스트 신규 10개 항목 전부 반영 확인(safety confound 4.4 / 3차원 강건성 4.5 / 두 지표 불일치 4.7·5.1 / 과대해석 방지 4.7·5.4 / Wilcoxon 방법 명기 4.1 / 다중검정 미적용 명시 4.1 / 블라인딩 정직서술 4.3 / 순서 불균형 4.6 / judge 신뢰도 5.3 / 재현자료 경로 Appendix C).

**집필 시 남긴 정직한 서술**: 3.4에 실행 실패 이력 전부 기록(세션한도 2회 실패, 배치 필터 버그, 데이터 손상 버그와 방어코드 추가, 연결오류 재시도). 캡션에는 그림 설명만 넣고 실패 서사는 본문 문단으로 분리(skill 3절 규칙).

**남은 작업(사용자 판단)**: References 서지정보(권/호/페이지) 원문 대조 — 논문 내 각주로 명시해둠. 그 외 집필 완료.

## ★★★ [2026-07-31] 논문 v3 전면 재작성 — 독립 검토 5개 관점 반영 ★★★
사용자가 v2를 읽고 "별로인 것 같다"며 재작성을 지시(`/goal`). ultracode(xhigh) 활성화 상태에서 진행.

**v2의 진짜 결함 (독립 검토 5레인이 찾아냄: 화학·통계·문체·규칙준수·과대주장)**
- blocker 6건, major 12건. 검토 지적을 전부 직접 재계산해 **모두 사실로 확인**했다. 특히:
  1. **블라인딩 측정이 순환적이었다** — "더 숙고돼 보이는 쪽" 지목은 조건 식별이 아니고, 네 항목 점수와 같은 생성에서 나왔다. → **별도 호출 식별 검사 20회를 새로 실행**(루브릭 문구 제거 자료, 채점 금지). 결과 20/20, p=1.9e-6, 확신도 전부 "높음". blocker가 실증으로 해결됨.
  2. **장문 편향 기제가 null이었다** — 합산 r=0.458은 조건B가 더 길고 동시에 더 높은 점수를 받았다는 사실만으로 생기는 값. 조건 내부는 A r=0.091(p=0.70), B r=0.216(p=0.36)이고 합산 기울기로는 격차의 26%만 설명. **v2의 핵심 해석이 틀렸다.** → 기제를 **심의 잔여 표지**로 교체(조건B 3.05회 대 조건A 0.40회, 18/20, 정확검정 p=7.6e-6). 식별 판정자들이 실제로 든 단서와 일치. 한 건은 최종 응답에 "PI"가 그대로 남아 있었음.
  3. **화학 answer key 오류 2건** — T8(대칭 다이올 모노아세틸화)에서 당량 기준을 0.9~1.0으로 잡았으나 통계적 선택성이므로 **저당량이 표준**(이항모델: 0.3당량 11:1 vs 1.0당량 2:1). T7 용매를 DCM계로만 열거했으나 THF/물·MeOH도 Boc2O 보호 통용 매질. **두 조건 다 부당 감점**이었음 → 키 정정·재채점: A 31/32, B 31/32, 격차 0.000(정정 전 −0.125).
  4. "격차의 대부분이 safety" → 실제 **46.9%**(과반 아님). "차이가 사라진다" → **"관찰되지 않았다"**(동점 6/8, 검정력 없음). "판정자를 바꿔도" → **같은 계열 모델의 독립 호출**.
  5. k 관계가 **비단조**(k2<k3,k4), 거친 대비 p=0.315, **조작 점검 실패**(조건A 절대점수가 k와 함께 상승, r=0.447 p=0.048).
  6. 과제 서술 자체의 화학 결함 3건 발견(T14 프리델-크래프츠 다이아실화는 자기제한적이라 일어나지 않음, T18 컨로테이토리는 궤도대칭이 결정, T20 레트로엔은 알콕사이드 조건에서 억제).
  7. 고k 프롬프트가 조작 변수를 명시해 **정답을 노출**(k≥3), 과제 형식도 설계형→설명형으로 변함 → H2 반전의 대안 설명.

**v3에서 새로 한 것**
- 조건 식별 검사(신규 20회 호출) + 심의 잔여 표지 측정 → `identification_probe.json`, `surface_features.json`
- 판정자 재채점(신규 20회) → `judge_reliability.json` (항목 ICC 0.703 / Gap ICC 0.359, 부호역전 2건)
- 부트스트랩 CI, leave-one-out, Spearman, Fisher, 정확검정/근사 규칙 일관 적용
- 그림 6개 신규 제작(dataviz 팔레트 검증 통과, 렌더링 눈으로 확인해 버그 5건 수정)
- **배제하지 못한 경쟁 설명 4개를 논문에 명시**(팀이 실제로 더 안전, 자기선호 편향, 순서 편향, 정답 노출) — v2는 "가장 설명력 있는 해석"이라며 2개만 검토하고 기각했었음
- 한계 11개(실측) + 4개(예상)로 확장, 호출 집계 228회로 정정

**FOL 확장**: FOL-5a(식별 검사), 6a/6b/6c 추가. FOL-6c에 "조건 내부 상관 필수" 조건 명시. FOL-10 주장문을 기제 교체에 맞춰 재작성(포괄성·상세함 → 심의 흔적).

**최종 상태**: 논문 507행. 수치 43개 항목 데이터 대조 통과, 그림 참조 6개 파일 일치, 취소선 0, 결론 3문장.
**남은 것**: 참고문헌 서지정보 원문 대조, 인간 전문가 채점 대조(후속 과제).

## ★★★ [2026-07-31] 앵커 논문 갭 분석 완료 — 논문 수정 착수 전 상태 ★★★

**현재 위치: gap_analysis.md 완성·push 완료. 논문(`논문_4주차_최종.md`)은 아직 손대지 않음(사용자 지시).**

### 왜 이 작업을 했나
사용자가 v3 논문을 읽고 지적: "FOL 내용이 왜 논문에 들어가 있나", "원래 논문과 구조·느낌이 너무 다르다". 둘 다 사실이었음.
- `FOL` 문자열 14회가 **전부 표 3 안에만** 있고 본문 사용 0회. 아무도 참조하지 않는 형식을 색인한 표를 방법 절 맨 앞에 뒀음.
- 참고문헌 아래 **저자용 할 일 메모**, 방법 절에 **커밋 메시지 수준 디버깅 일지**, 절 상호참조 32회, 파일경로 인용 13회, 본문 인용 표시 0회, `우리` 1회 대 `본 연구` 38회.

### 조사 방법과 결과
앵커 원문(bioRxiv 전문 + Nature 출판본)을 직접 조회해 기준으로 삼고 **8관점 독립 심사 병렬 실행**. 8/8 전부 `diverges_harmfully`, **갭 91건(must_fix 39·should_fix 42·optional 10)**.
- 한 줄 진단: **앵커는 청중에게 하는 보고, 우리 원고는 심사자에게 내는 감사 보고서.**
- 근본 원인 5개: ①한계 질량이 발견 압도(논의+한계 4,498자 중 자기제한 2,740자=60.9%) ②독자를 채점자로 대우 ③장치가 실질보다 많음(방법 21.8% vs 결과 23.3%, 에이전트 발언 인용 0건) ④첫 결과 인상이 자기 유죄 판정(결과 제목 9개 중 8개 문장형, 6개 부정) ⑤서사·행위자 부재

### ★ 가장 중요한 발견 — skill이 발판을 요구했다
`paper_writing_skill.md` **78행 체크리스트**가 "Method 소절 제목이 FOL 단계와 1:1 대응하는지 **표로 대조했는가?**"를 요구함. 논문 최대 결함이 규칙 위반이 아니라 **규칙의 산물**이었음. 정면 충돌 9건(K1~K9) + **skill 사실오류 1건**(52행이 "앵커의 7. 한계점처럼"이라고 적었으나 **앵커에는 한계 절이 없다** — 독립 한계 절 22항목의 근본 원인).
→ **skill 개정이 논문 개정의 선행 조건.** 안 고치면 논문 수정과 체크리스트가 서로를 되돌림.

### 3트랙 구조로 정리 (gap_analysis.md에 전부 문서화)
- **트랙 A (선행, 작음)**: skill 개정 — 기존 규칙 9건 삭제·수정 + 사실오류 1건 + 신규 규칙 10건(N1~N10)
- **트랙 B (큼)**: 원고 제시 개정 — must_fix 39건(S1~7/A1~3/R1~6/M1~5/F1~6/C1~7/V1~5) + should_fix 42건 + 그림 7개 자리 명세 + 표 X·Y 신설
- **트랙 C (재실행 필요, 결정 대기)**: 타당도 7건(P1~P7)

### ★ 사용자 결정이 필요한 것 2개
1. **H2의 지위** — k 조작 점검이 실패했고(조건A 절대점수가 k와 함께 상승, r=0.447 p=0.048) 고k 프롬프트가 정답을 노출함. H2를 가설 검정으로 유지할지 탐색적 관찰로 격하할지. **현재 자료로는 격하가 더 정직함.**
2. **트랙 C 실행 여부** — 과제 3건(T14/T18/T20 전제 화학 결함) 교체 + 프롬프트 수정 후 재실행은 비용 큼. 안 하면 "잘 쓰인 논문이지만 H2는 탐색적"으로 마감.
→ 트랙 B는 이 결정과 무관하게 지금 시작 가능.

### 이번 작업에서 데이터도 보강함
`identification_probe.json`에 판정자 단서 원문이 저장 안 돼 있던 것을 발견해 워크플로 로그에서 회수. `cue`·`cue_category`·`cue_coding` 추가. 범주: 대안 기각·배제 5 / 유보·미검증 5 / 정정·철회 4 / 반론 흡수 4 / 역할표지 1 / 이질적 검토층 1 = 20건 전수.

### gap_analysis.md 이력 (4판)
(1) 8관점 조사·갭 91건 → (2) 그림 자리 명세 추가 → (3) skill 충돌·타당도 결함 추가 → (4) **전체 재조립**(결론을 맨 앞으로, 흩어진 개정 지시를 3트랙으로 묶고, 중복 절 통합, 진행 추적 체크리스트 부록 추가). 591행.
모든 정량 주장을 원본 파일로 직접 검산했고, 검산에서 어긋난 4건은 실측값으로 정정함.

### [업데이트] 트랙 A 완료 + H2 결정 완료 (2026-07-31)
- **사용자 결정: H2를 탐색적 관찰로 격하.** 사전 지정 가설은 H1 하나. 이로써 트랙 C의 P2·P3(H2용 재실행) 불필요. P1(과제 3건 화학 전제 결함)은 여전히 남음 → 결정 대기.
- **트랙 A 실행 완료** — `paper_writing_skill.md` 개정:
  - K1~K9: FOL 대응표 체크 항목 삭제, Method-FOL 대응 규칙 삭제(→"기획 단계에서만, 본문 노출 0"), 한계 이분법 삭제(→Discussion 산문+완화책), 용어해설 필수→선택·통계용어 금지, 질문형 제목→선언형, 5단계 1:1 대응표 필수 삭제, 분석계획→Method 하위절, Conclusion 필수→선택
  - K10: 사실오류 정정 — "앵커의 7. 한계점처럼"은 거짓(앵커엔 한계 절 없음). "앵커처럼" 쓰기 전 원문 확인 규칙 추가
  - N1~N10: 8절 신설(발판 금지·한계 예산·단일 정착지·행위자·결과 제목·그림 우선·인용·출판본 확인·미완 금지·재현 사양) — 각 규칙에 검증 방법 컬럼 부착
  - 재점검에서 발견한 추가 모순 9건도 수정: 체크리스트 "제목이 질문형인가"/"1:1 대응표로 제시됐는가"(방금 고친 규칙과 충돌), 낡은 문헌일치 수치(30/32·29/32 → 31/31·gap 0.000), **"judge 신뢰도 측정 불가"라는 이미 반증된 항목**(재채점으로 측정했음: ICC 0.359), 재현자료 경로 항목(→가용성 진술), H2 격하 결정 반영
- **gap_analysis.md 5판**: 진행 상태 절 신설, 트랙 A ✅ 표시, 8.1절에 격하 결정과 그것이 트랙 B에 주는 영향 5곳 명시, 트랙 C 체크리스트에서 P2·P3 해소 처리

### [업데이트] 트랙 B 완료 — 논문 전면 재작성 (2026-07-31)
`/goal`로 "gap-analysis에 있는 거 싹 다 고치고 한국어로" 지시받아 실행. **논문을 재작성했다(412행 6,566단어, 이전 507행 7,111단어).**

**그림 7개 새로 제작** (`그림생성.py` 전면 재작성, 파일명=본문 번호):
- 그림1 설계도식 **재제작** — 2패널. (a)에 조건B 7회 호출의 입력 화살표(③⑤는 2건, ⑦은 6건 누적)를 그려 정보량 비대칭을 시각화. (b) 평가 3경로. 렌더링 확인해 텍스트 넘침·화살표 엉킴 3건 수정
- 그림2 **신설** — 판정자 단서 범주 막대(5/5/4/4/1/1)
- 그림4 **재설계** — 항목별 평균막대(표와 중복)→4항목 점수 분포 2×2. 안전성만 조건A가 2~3점에 17건, 조건B가 5점에 18건으로 이분화되는 게 보임
- 그림5 T19·T20 라벨+부호불일치 사분면 음영, 그림6 패널 순서 교체(지지된 기제를 앞으로), 그림3·7 캡션 재작성

**논문 구조 변경**: 한계 절 해체→논의 5.3에 산문 3문단(각 항목에 완화책 부착) / 분석계획→방법 3.6 흡수 / 결론 절 삭제(5.5 종합주장이 닫음) / 결과 9소절→7소절 전부 중립 명사구 / FOL 대응표·앵커 5단계 대응표 삭제 / 저자용 메모 삭제 / 디버깅 일지→부록 C / 파일경로 인용→데이터·코드 가용성 진술 / 참고문헌 12건 완성(앵커를 Nature 646, 716–723 (2025)로 교체) + 본문 인용 13회 삽입 / 초록 재작성(숫자 3개) / 1인칭 전환(우리 29회 vs 본 연구 2회, 이전은 1 대 38) / 감사의말·저자기여·이해상충 신설 / 용어해설 화학만 남김

**완료 기준(6.10) 전부 통과**: FOL 0, 파일경로 0, 저자용메모 0, 취소선 0, 미래형 0, 규칙명 0, 상호참조 8(기준 ≤8), 본문인용 13(기준 ≥10), **자기제한/주장 비율 1.00(기준 ≤1.20)**, 그림 7개 전부 본문 호출+캡션 n·척도, 결과 제목 전부 명사구, 참고문헌 완성. 수치 21개 원자료 재대조 전부 일치.

**신규 산출물**: `실험재현자료/과제별_결과_일람.md`(20건 × 10열) — 부록 C가 가리키는 표를 실제로 생성

**남은 것**: 트랙 C의 P1(과제 T14/T18/T20 화학 전제 결함) 결정, P4~P7. 나머지는 논문 5.3절에 완화책과 함께 명시함.

### 다음 세션에서 이어갈 때
1. `gap_analysis.md` **결론 절만 읽으면 전체 파악 가능** (맨 앞 30행)
2. 사용자 결정 2개 확인
3. **트랙 A·B 완료됨.** 남은 것은 트랙 C(P1·P4~P7) 결정뿐
4. 부록 진행 추적 체크리스트에 ☐ 체크하며 작업
5. **논문은 v4 — 갭 91건 반영 완료**

## 다음에 할 일
- 연구(실험+결과자료+앱 데이터)는 완료됨. **논문 작성은 사용자가 별도로 진행**(이 세션에서 손대지 말 것).
- lab_app UI: 실제 CC0 캐릭터 스프라이트 교체 + 배회/이름표 버그 수정까지 완료, 스크린샷 자체검증도 마침. **다음은 사용자의 실제 확인** — 화면 보고 여전히 부족한 부분이 있으면 그 구체적 피드백을 받아서 진행.

## 지금까지 누적 결과 (배치1+2+3 전부, 12/20건 유효)
- 전부 B우위(12/12), 전부 블라인딩 뚫림(12/12) — k=1,2,3 구간까지 매우 일관된 패턴
2. 20건 다 모이면: (a) 하나의 data/transcripts.json으로 병합 (b) H1(Wilcoxon)·H2(k회귀) 통계 계산 (c) lab_app 재검증 (d) 결과 리포트 Artifact 갱신 (e) ultragoal G001→complete 체크포인트
3. k=1~2 문헌일치 판정 기준 세부사항 확정 필요
4. **2단계(앵커보완)·3단계(초안작성)는 이번 세션 스코프 밖 — 진행하지 말 것**

## 참고
- 3주차 산출물(3주차/논문_조건민감_화학설계_비판루프멀티에이전트_완성본.md, gap-analysis.md, pilot_data/)을 그대로 재활용 가능
- 3주차에서 발견된 구조적 문제(조건B 셀프시뮬레이션 대체, 블라인딩 실패)는 4주차 설계 시 처음부터 피해야 함
